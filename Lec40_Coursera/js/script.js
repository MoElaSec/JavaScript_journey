//WHy we need Namespaces

// sayHello(); //?Out: Hello, Mo
// sayHi(); //?Out: Hi, Mo

shifraGretter.sayHello();
moGretter.sayHi();

//! Immediately Invoked Function Expressions (IIFEs)
//? () -> inside them is an expression
//? this exp is a function 
//? we use () at the end to invoke it 
//? this is how the name exsits 

// (function (name) {
//     console.log("Hello, " + name + "!")
// })("GitHub");

//TODO: Remember Immediately Invoked Function Expressions (IIFE) 
//TODO: are usually used to place code into its own execution context not to conflict with the global scope.

